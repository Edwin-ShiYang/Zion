packet types
- reliable-ordered
- unreliable

resends reliable packets until they are received by the other side


messages are smaller bitpacked

```C++
struct Header
{
    uint16_t sequence;     //  is a number that increases with each packet sent
    uint16_t ack;          //  is the most recent packet sequence number received
    uint32_t ack_bits;     //  a bitfield encoding the set of acked packets
};
```


